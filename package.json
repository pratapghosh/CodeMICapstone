{
    "name": "todo-app-ui-tests",
    "private": true,
    "scripts": {
        "ui:install": "playwright install chromium",
        "ui:test": "playwright test --config=src/test/ui/playwright.config.js",
        "ui:test:headed": "playwright test --config=src/test/ui/playwright.config.js --headed",
        "ui:test:debug": "playwright test --config=src/test/ui/playwright.config.js --debug"
    },
    "devDependencies": {
        "@playwright/test": "^1.54.1"
    }
}